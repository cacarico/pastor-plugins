#!/bin/sh
# A stand-in for gh: records its arguments and answers `api` from GH_FIXTURE,
# so the scripts can be checked without a GitHub login or the network.
printf '%s\n' "$@" > "$GH_ARGS"
case "$1" in
  api) cat "$GH_FIXTURE" ;;
esac
